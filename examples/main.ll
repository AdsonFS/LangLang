var cnt -> number;
var age -> number;
var name -> string;

if (0)
{
  var cnt -> number;
  >> cnt := 10;
} else {
  var cnt -> number;
  >> cnt := 80;
}

if(0) {
  >> "if";
}

cnt := age := 20;
>> cnt >> age >> "name: " + (name := "John");
